.report
  %h1.logo
    Hound
    %span Violations

  - build.violations.each do |violation|
    %h4#git_diff
      = link_to violation[:filename], '#git_diff'

    - violation[:violations].each do |violation|
      %ul
        %li.violation
          %span.line-number
            = violation[:line_number]
          %pre
            %code
              = violation[:code]
          .message
            %p
              = violation[:message]

%footer.report-repo-list
  .repo-listing
    %ul
      %li.repo
        %span.toggle.deactivate
          %a{'href' => ''}
        %span.name
          = build.repo.name
      %li.repo
        %span.name
          = link_to 'View all other repos', root_path
