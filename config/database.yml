development: &default
  adapter: postgresql
  database: hound_development
  encoding: utf8
  host: localhost
  min_messages: warning
  pool: 2
  timeout: 5000

test:
  <<: *default
  database: hound_test

production:
  url: <%= ENV.fetch("DATABASE_URL", "") %>ion:
